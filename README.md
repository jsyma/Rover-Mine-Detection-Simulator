# Rover Mine Detection Simulator

This program simulates the movement of multiple rovers on a grid-based map, where rovers can encounter mines. It fetches rover commands from an API, reads a map file, and disarms mines using a serial number and a 
PIN system. This program allows for both sequential and parallel processing of the rovers' commands using threading and multiprocessing. 
